<div class="container animated fadeInUp">
  {{#if model.length}}

  {{#link-to "cadastrar-marca"}} Cadastrar nova marca {{/link-to}}

  <div class="container">
    <h1>Listagem de marcas</h1>
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Marca</th>
          <th>Descrição</th>
        </tr>
      </thead>
      <tbody>
        {{#each model as |marca index|}}
        <tr>
          <td>{{marca.nome}}</td>
          <td>{{marca.descricao}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

  <div class="pagination-center text-center">
  {{bootstrap/dynamic-pagination
    class="pagination"
    actionOnClick="actPaginar"
    totalPaginas=pagination.total_pages
    paginaAtual=pagination.current_page
    mostrarProximoAnterior=true}}
  </div>

  {{else}}

  <div class="container">
      <div class="row">
          <div class="col-md-12">
              <div class="error-template">
                  <h1>
                      Não há marcas a serem listadas</h1>
                  <div class="text-center">
                      Nenhum registro foi encontrado.
                  </div>
                  <div class="error-actions">
                    {{#link-to "cadastrar-marca" class="btn btn-primary btn-lg"}} Cadastrar nova marca {{/link-to}}
                    {{#link-to "index" class="btn btn-default btn-lg"}} Voltar {{/link-to}}
                  </div>
              </div>
          </div>
      </div>
  </div>

  {{/if}}

</div>
